@model IEnumerable<LineaTelefonica.Entities.Entities.TipoLinea>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreTipoLinea)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreTipoLinea)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoLineaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoLineaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoLineaId })
        </td>
    </tr>
}

</table>
