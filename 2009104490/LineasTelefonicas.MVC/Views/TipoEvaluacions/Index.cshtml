@model IEnumerable<LineaTelefonica.Entities.Entities.TipoEvaluacion>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreTipoEvaluacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreTipoEvaluacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoEvaluacionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoEvaluacionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoEvaluacionId })
        </td>
    </tr>
}

</table>
