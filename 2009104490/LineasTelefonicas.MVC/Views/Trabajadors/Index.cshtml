@model IEnumerable<LineaTelefonica.Entities.Entities.Trabajador>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TipoTrabajador.TipoTrabajadorId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NombreTrabajador)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TipoTrabajador.TipoTrabajadorId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NombreTrabajador)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TrabajadorId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TrabajadorId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TrabajadorId })
        </td>
    </tr>
}

</table>
